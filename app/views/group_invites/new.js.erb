var button = $("#invite_button");
button.replaceWith(button.clone());
var button = $("#invite_button");

button.popover({trigger: "manual",
                placement: "below-right",
                offset: 10,
                html: true,
                title: function(){return "Invite Others";},
                content: function(){return "<%=j render partial: 'friend_form' %>";}})

button.popover("show");
$("select.chzn-select").chosen();
